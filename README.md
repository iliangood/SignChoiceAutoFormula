## сборка осуществляется через zig
```sh
zig build
```
Запустить можно с помощью:
```sh
sig build run -- #аргументы для программы
```
### Параметры сборки:
- `-Dmeasure-time` - засекает время вычеслений
- `-Dabstract` - использует другую формулу, без работы с числами, только строки
